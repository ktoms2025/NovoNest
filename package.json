{
  "name": "ai-prescreen-interview-tool",
  "version": "0.1.0",
  "private": true,
  "description": "AI Pre-Screen Interview Tool prototype — Phase 1 (text-only core loop)",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
